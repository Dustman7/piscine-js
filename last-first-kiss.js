function first (s) {
    return s[0]
    
    
    
    }
    
    function last (s) {
    return s[s.length - 1]
    
    
    
    }
    
    function kiss (s) {
        const fir = s[0]
        const len = s[s.length -1]
        const resu = [len, fir]
     return resu
    
        
    
    }